<%= form_with(model: answer_detail, local: true) do |form| %>
  <% if answer_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(answer_detail.errors.count, "error") %> prohibited this answer_detail from being saved:</h2>

      <ul>
      <% answer_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
