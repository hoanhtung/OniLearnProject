<p id="notice"><%= notice %></p>

<h1>Exam Details</h1>

<table>
    <% @exam_details.each_with_index do |exam_detail, index| %>
      <tr>
        <td>Câu hỏi <%= index + 1 %>: </td>
        <td> </td>
      </tr>
      <tr>
        <td><%= exam_detail.question.content %> </td>
      </tr>
    <% end %>
</table>
