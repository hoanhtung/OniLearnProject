<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @course.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @course.description %>
</p>

<p>
  <strong>Icon:</strong>
  <%= @course.icon %>
</p>

<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', subject_courses_path(@course.subject_id) %>
